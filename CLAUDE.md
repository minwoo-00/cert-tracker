# 기술 규칙

- 순수 HTML/CSS/JS로만 구현한다. (프레임워크·라이브러리·빌드 도구 사용 금지)
- 파일은 index.html, style.css, app.js 3개로만 유지한다. (추가 파일 생성 금지)
- 데이터는 서버 없이 브라우저 localStorage에 저장한다.

# 작업 규칙

- 한 번에 한 태스크만 진행한다.
- 변경 후에는 반드시 브라우저에서 직접 확인한다.
- 요구사항은 PRD.md를 참조한다.
